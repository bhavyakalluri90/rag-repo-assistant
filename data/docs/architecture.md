# Project Architecture

This application is a checkout system built with React and TypeScript.

The checkout page is responsible for displaying cart details, selecting payment methods, and submitting payment requests.

Payment logic is handled in the payment service.

Reusable UI components live in the components folder.